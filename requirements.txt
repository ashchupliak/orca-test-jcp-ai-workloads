# Core test framework dependencies
psutil>=5.9.0
requests>=2.31.0

# Grazie client dependencies
typing-extensions>=4.0.0

# AI workload dependencies (for 90-ai-workloads tests)
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0

# Optional: GPU support (uncomment if GPU available)
# torch[cuda]>=2.0.0

# Optional: API server (uncomment if testing API endpoints)
# fastapi>=0.100.0
# uvicorn>=0.23.0

# Optional: GPU monitoring (uncomment if GPU available)
# GPUtil>=1.4.0
